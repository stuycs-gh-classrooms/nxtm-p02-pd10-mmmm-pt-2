class Tetronimo {
  int state;
  int x_cor;
  PVector pos;

  Tetronimo(int s, int x) {
    x_cor = x;
    state = s;
  }

  void display() {//here is where all the shape code will go.
    //please keep x, y the same, dont add anything to them
    if (state == O_BLOCK) {
    } else if (state == I_BLOCK) {
    } else if (state == J_BLOCK) {
    } else if (state == L_BLOCK) {
    } else if (state == T_BLOCK) {
    } else if (state == S_BLOCK) {
    } else if (state == Z_BLOCK) {
    }
  }
}
